name: Test get job id
on:
    push:
        branches:
            - main
    pull_request:

defaults:
    run:
        shell: bash
            

jobs:
    test:
        runs-on: ${{ matrix.os }}
        strategy:
            matrix:
                os: [macos-latest, windows-latest, ubuntu-latest]
                version: [1.1.0, 1.0.0]
        name: a job to get id
        steps:
            - name: Checkout
              uses: actions/checkout@v3

            - name: Use action
              id: job
              uses: ./
              with:
                job-name: other name

            - name: get putput
              run: echo "the job id is ${{ steps.job.outputs }}"